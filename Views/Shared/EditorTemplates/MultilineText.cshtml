@{
    var value="";
    if(ViewData.TemplateInfo.FormattedModelValue!=null)
    {
        value=ViewData.TemplateInfo.FormattedModelValue.ToString();
    }
    }
@Html.TextArea("", value, new { @class="form-control",rows="8"})